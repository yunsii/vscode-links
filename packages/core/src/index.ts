// Placeholder. PR 2 will move provider parsing, template engine, CSV
// resolution, and the resolve() entry point in here, then the VSIX will
// import from `@vscode-links/core` instead of from `src/`.
export {}
